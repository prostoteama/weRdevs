export const isOpen = (state) => {
  return state.data.isOpen;
};

export const getMonth = (state) => {
  return state.data.cred.month;
};

export const getDay = (state) => {
  return state.data.cred.day;
};
